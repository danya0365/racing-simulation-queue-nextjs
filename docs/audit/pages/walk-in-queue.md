# 🔍 รายงานการตรวจสอบ: ระบบคิว Walk-in (Walk-in Queue)

**สถานะ**: ⚠️ ต้องปรับปรุง UI (Backend logic พร้อมใช้งาน 100%)
**ผู้ตรวจสอบ**: Antigravity (AI Assistant)
**วันที่**: 24 มกราคม 2026

---

## 🏗️ ภาพรวม
ระบบคิว Walk-in ถูกออกแบบมาเพื่อรองรับลูกค้าที่หน้าร้านโดยไม่ต้องจองล่วงหน้า มีการคำนวณลำดับคิวและเวลารอแบบอัตโนมัติผ่าน Database RPCs

---

## 📡 การเชื่อมต่อ API และฐานข้อมูล

| ฟังก์ชัน / ส่วนประกอบ | ปลายทาง | ประเภท | รายละเอียด | สถานะความปลอดภัย |
| :--- | :--- | :--- | :--- | :--- |
| `rpc_join_walk_in_queue` | RPC Function | `EXECUTE` | [Customer] กดเข้าคิวหน้าร้าน | **ปลอดภัย**: รองรับ GUEST |
| `rpc_get_waiting_queue` | RPC Function | `EXECUTE` | [Staff] ดึงรายการคิวที่รออยู่ | **ปลอดภัย**: Mask เบอร์โทร |
| `rpc_seat_queue_customer`| RPC Function | `EXECUTE` | [Staff] จัดที่นั่งและเริ่มเล่น | **ปลอดภัย**: เฉพาะพนักงาน |
| `rpc_get_my_walk_in_queue`| RPC Function | `EXECUTE` | [Customer] ดูลำดับคิวตัวเอง | **ปลอดภัย**: กรองรายบุคคล |

---

## 🔒 ผลการตรวจสอบและประเด็นที่พบ

### 1. โครงสร้างฐานข้อมูล (Backend Infrastructure) ✅
- **Phase 2 Migration**: ฐานข้อมูลมีการออกแบบใหม่ที่สมบูรณ์ (Table: `walk_in_queue`) รองรับสถานะ `waiting`, `called`, `seated`, `cancelled`
- **Session Integration**: ระบบเชื่อมต่อกับตาราง `sessions` ได้อย่างถูกต้องผ่าน `rpc_start_session`
- **Security Definitions**: ทุก RPC สำคัญมีการใช้ `SECURITY DEFINER` และตรวจสอบสิทธิ์ภายในฟังก์ชันอย่างรัดกุม

### 2. ส่วนติดต่อผู้ใช้งาน (UI/UX) ❌
จากการ Deep Dive พบประเด็นความไม่สอดคล้อง (Mismatch) ของ UI หลายจุด:
- **Missing Join Page**: ไม่พบหน้าสำหรับให้ลูกค้ากดเข้าคิว (Join Queue) แยกออกมาต่างหากในปัจจุบัน
- **Terminology Confusion**: หน้า `MyQueueStatusView` และ `QueueStatusView` ใช้คำว่า **"Booking/การจอง"** กับข้อมูลที่เป็น Walk-in ทำให้ลูกค้าสับสนได้
- **Legacy Components**: โค้ด UI ปัจจุบันพยายามใช้ร่วมกับระบบ Advance Booking (รุ่นเก่า) ทำให้โครงสร้างข้อมูลไม่ตรงกับ RPC ใหม่ (เช่น คาดหวังฟิลด์ `bookingTime` ทั้งที่ควรใช้ `joinedAt`)

### 3. ระบบแอดมิน (Admin Dashboard) ⚠️
- **Data Schema Mismatch**: `QueuesTab` ในหน้า Dashboard ยังดึงค่า `duration` และ `bookingTime` มาแสดงผล ซึ่งไม่มีอยู่ในชุดข้อมูล Walk-in ของ RPC ใหม่ ส่งผลให้การแสดงผลอาจผิดพลาด

---

## 💡 ข้อเสนอแนะ (Recommendation)
> [!IMPORTANT]
> **ควรลบโค้ด UI ในส่วนของ Queue หน้าบ้านทิ้งและเขียนใหม่ (ตามที่คุณเสนอ)** โดยแยก Logic ระหว่าง Advance Booking และ Walk-in Queue ให้ชัดเจน:
> 1. สร้างหน้า `/walk-in` สำหรับเข้าคิวใหม่โดยเฉพาะ
> 2. ปรับปรุง `QueuesTab` ใน Dashboard ให้รองรับฟิลด์ข้อมูลจาก `rpc_get_waiting_queue` ตัวใหม่
> 3. แก้ไข Terminology ในหน้าติดตามสถานะจาก "การจอง" เป็น "ลำดับคิว"

---
*หมายเหตุ: โครงสร้าง Backend (SQL/RPC) แข็งแกร่งมากแล้ว สามารถใช้ต่อได้ทันทีโดยไม่ต้องแก้ไข*
