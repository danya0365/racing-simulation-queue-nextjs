# 🔍 รายงานการตรวจสอบ: หน้าโปรไฟล์ (Profile Page)

**ไฟล์ที่ตรวจสอบ**: `app/profile/page.tsx`
**สถานะ**: ⚠️ พบช่องโหว่ระดับสูง (High Risk - Data Leakage)
**ผู้ตรวจสอบ**: Antigravity (AI Assistant)
**วันที่**: 24 มกราคม 2026

---

## 🏗️ ภาพรวม
หน้าโปรไฟล์ใช้สำหรับให้ผู้ใช้ที่เข้าสู่ระบบ (Authenticated Users) จัดการข้อมูลส่วนตัว เช่น ชื่อ-นามสกุล, เบอร์โทรศัพท์ และข้อมูลการตั้งค่าอื่นๆ

---

## 📡 การเชื่อมต่อ API และฐานข้อมูล

| ส่วนประกอบ / ฟังก์ชัน | ปลายทาง | ประเภท | รายละเอียด | สถานะความปลอดภัย |
| :--- | :--- | :--- | :--- | :--- |
| `authRepository.getSession()` | Supabase Auth | `GET` | ตรวจสอบ Session การเข้าสู่ระบบปัจจุบัน | **ปลอดภัย** |
| `authRepository.getProfile()` | Table: `public.profiles` | `SELECT` | ดึงข้อมูลชื่อและเบอร์โทรของผู้ใช้ | ⚠️ **เสี่ยงมาก** (อ่านต่อด้านล่าง) |
| `authRepository.updateProfile()`| Table: `public.profiles` | `UPDATE` | บันทึกการแก้ไขข้อมูลส่วนตัว | **ปลอดภัย**: เช็ค UID ตรงกัน |
| `authRepository.signOut()` | Supabase Auth | `EXECUTE` | ออกจากระบบ | **ปลอดภัย** |

---

## 🔒 การตรวจสอบความปลอดภัยและช่องโหว่

### 1. ช่องโหว่การรั่วไหลข้อมูลส่วนบุคคล (Data Leakage) 🚨
จากการตรวจสอบไฟล์ Policy ใน `20250618000001_security_policies.sql`:
```sql
CREATE POLICY "Public profiles are viewable by everyone"
  ON public.profiles FOR SELECT
  USING (true);
```
**ปัญหา**: นโยบายนี้อนุญาตให้ **ใครก็ได้ (รวมถึงคนทั่วไปที่ยังไม่ได้ Login)** สามารถดึงข้อมูล "ทุกคน" ในตาราง `profiles` ออกมาดูได้ทั้งหมดผ่าน API หรือคำสั่ง SQL

**ข้อมูลที่รั่วไหล**:
- เบอร์โทรศัพท์ของผู้ใช้งานทุกคน
- ชื่อ-นามสกุลจริง
- ที่อยู่ (หากมีการกรอก)
- ข้อมูลทางสถิติและการตั้งค่าอื่นๆ

### 2. การสร้างข้อมูล (Create Operation)
- ระบบมีการใช้ Trigger `handle_new_user` เพื่อสร้าง Profile ให้อัตโนมัติเมื่อมีการ Signup ซึ่งเป็นวิธีที่ปลอดภัยและเป็นมาตรฐานของ Supabase

---

## 💡 ข้อเสนอแนะและมาตรการแก้ไข
- **ความเร่งด่วน**: **สูงมาก** แนะนำให้รีบแก้ไข RLS ของตาราง `profiles` ทันที
- **แนวทางแก้ไข**: ปรับปรุงนโยบาย `SELECT` ให้ผู้ใช้เห็นได้เฉพาะข้อมูลของตนเองเท่านั้น:
  ```sql
  -- ตัวอย่างการแก้ไข
  CREATE POLICY "Profiles are viewable by owners or admins"
    ON public.profiles FOR SELECT
    USING (auth.uid() = auth_id OR public.is_moderator_or_admin());
  ```

---
*หมายเหตุ: แม้หน้าเว็บ (UI) จะแสดงเฉพาะข้อมูลของเจ้าของ แต่ช่องโหว่นี้ยอมให้ Hacker สามารถดูข้อมูลคนอื่นผ่านช่องทาง API ได้โดยตรง*
