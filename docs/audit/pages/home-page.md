# 🔍 รายงานการตรวจสอบ: หน้าแรก (Home Page)

**ไฟล์ที่ตรวจสอบ**: `app/page.tsx`
**สถานะ**: ✅ พร้อมใช้งาน (ผ่านการตรวจสอบความปลอดภัย)
**ผู้ตรวจสอบ**: Antigravity (AI Assistant)
**วันที่**: 24 มกราคม 2026

---

## 🏗️ ภาพรวม
หน้าแรกทำหน้าที่เป็น Landing Page ของลูกค้า เพื่อแสดงสถานะล่าสุดของเครื่องเล่น สถิติการจอง และคิวที่รออยู่ของวันปัจจุบัน

---

## 📡 การเชื่อมต่อ API และฐานข้อมูล

| ส่วนประกอบ / ฟังก์ชัน | ปลายทาง | ประเภท | รายละเอียด | สถานะความปลอดภัย |
| :--- | :--- | :--- | :--- | :--- |
| `SupabaseMachineRepository.getAll()` | Table: `public.machines` | `SELECT` | ดึงรายชื่อเครื่องและสถานะปัจจุบัน | **ปลอดภัย**: ควบคุมด้วย RLS |
| `rpc_get_waiting_queue` | RPC Function | `EXECUTE` | ดึงคิวที่รออยู่ (มีการ Mask เบอร์โทร) | **ปลอดภัยมาก**: ซ่อนข้อมูลในระดับ DB |
| `rpc_get_walk_in_queue_stats` | RPC Function | `EXECUTE` | ดึงสถิติภาพรวมคิวของวันนี้ | **ปลอดภัย**: มีเฉพาะข้อมูลตัวเลขสรุป |
| `rpc_get_bookings_schedule` | RPC Function | `EXECUTE` | ดึงผังเวลาการจองของแต่ละเครื่อง | **ปลอดภัย**: ซ่อน ID และเบอร์โทรลูกค้า |
| `rpc_get_bookings_by_machine_date` | RPC Function | `EXECUTE` | ดึงรายละเอียดการจองรายเครื่อง | **ปลอดภัย**: ป้องกันการรั่วไหลข้อมูล PII |

---

## 🔒 การตรวจสอบข้อกำหนดความปลอดภัย

### 1. ความเป็นส่วนตัวของข้อมูล (PII)
- **ชื่อลูกค้า**: เปิดเผยต่อสาธารณะเพื่อความโปร่งใสในตารางเวลา
- **เบอร์โทรศัพท์**: ถูก **ซ่อน (Hidden)** หรือ **พรางข้อมูล (Masked)** โดยอัตโนมัติผ่าน Logic ใน RPC สำหรับผู้ใช้งานทั่วไป
- **ไอดีลูกค้า (Customer ID)**: ถูกซ่อนอย่างสมบูรณ์ผ่าน RLS Policy ที่เราปรับปรุงใหม่

### 2. การควบคุมการเข้าถึง (Access Control)
- ไม่อนุญาตให้บุคคลทั่วไปดึงข้อมูลจาก Table โดยตรง ทุกการเรียกใช้บนหน้าเว็บนี้ผ่าน RPC ที่ทำหน้าที่เป็น "Secure Proxy"
- ผู้ใช้งานทั่วไปไม่สามารถทำการ `INSERT`, `UPDATE` หรือ `DELETE` ข้อมูลจากหน้านี้ได้

---

## 💡 ข้อเสนอแนะเพิ่มเติม
- **ไม่ต้องแก้ไขเพิ่มเติม**: การทำงานในปัจจุบันเป็นไปตามหลัก "Security by Design" โดยใช้ RPC เป็นตัวกลางและมี RLS เป็นเกราะป้องกันชั้นนอก
